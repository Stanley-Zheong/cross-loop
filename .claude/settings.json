{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) command -v ruff >/dev/null && ruff check --fix \"$f\"; command -v black >/dev/null && black \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Python (ruff + black)..."
          }
        ]
      }
    ]
  }
}
